<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="dislMagicGarden.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:dislMagicGarden"
    xmlns:views="clr-namespace:dislMagicGarden.Views"
    Shell.NavBarIsVisible="False"
    Title="dislMagicGarden">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

    <ShellContent 
        Title="Start"
        ContentTemplate="{DataTemplate views:HomePage}"
        Route="HomePage" />

    <ShellContent 
         Title="Start"
         ContentTemplate="{DataTemplate views:ChapterEditorPage}"
         Route="ChapterEditorPage" />

    <ShellContent 
        Title="Neue Geschichte"
        ContentTemplate="{DataTemplate views:NewStoryPage}"
        Route="NewStoryPage" />

</Shell>
